# Recording captures (event 2030 only)

Frozen public HTML from the proven hosted run. Open these as local files in the
browser. **Do not click Run Authority Assessment on the live URL** — a new
click would mint a new event bucket, not replay 2030.

| File | What it is |
|---|---|
| `demo-landing.html` | Anonymous GET `/p2/demo` landing |
| `demo-2030-live.html` | First 2030 result: LIVE ASSESSMENT, COMPLETED |
| `demo-2030-replay.html` | Same event replay: CACHED — IDEMPOTENT REPLAY |

Exclude: `judge-demo-v1-20260911-2055`.
